# Infra

This directory is intentionally minimal. Local PostgreSQL is defined in the root `docker-compose.yml`, and additional infrastructure artifacts should only be added here when they are justified by real deployment or operations needs.
